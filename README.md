Snow-Fort Server

Uses (chibi net http-server).  You can run with:

    chibi-scheme -Rchibi.net.http-server -- config.scm

Default port is 8000. To update the repositories run:

    sh cron.sh

the cron.sh needs to be run in programs root directory.
